<!DOCTYPE html>
<html lang="pt">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Lista de Pagamentos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        .rodape {
            text-align: center;
            position: fixed;
            width: 100%;
            bottom: 0;
            background-color: #f8f9fa;
            padding: 10px 0;
        }
    </style>
    
    <script>
        function toggleInfo(id) {
            let row = document.getElementById("info-" + id);
            row.classList.toggle("d-none");
        }
    </script>
</head>

<body>
    <div class="container py-4">
        <h1 class="text-center">Setor Administrativo - Titanium</h1>
        <br>

        <!-- Abas de Navegação -->
        <ul class="nav nav-tabs mb-4">
            <li class="nav-item"><a class="nav-link" href="http://localhost:3000/cliente/listar">Clientes</a></li>
            <li class="nav-item"><a class="nav-link" href="http://localhost:3000/funcionario/listar">Funcionários</a></li>
            <li class="nav-item"><a class="nav-link active" href="http://localhost:3000/pagamento/listar">Pagamentos</a></li>
            <li class="nav-item"><a class="nav-link" href="http://localhost:3000/horarios/listar">Horários</a></li>
        </ul>

        <table class="table table-striped table-bordered">
            <a href="http://localhost:3000/pagamento/add" class="btn btn-primary mb-3">Adicionar Pagamento</a>
            <thead>
                <tr>
                    <th scope="col">Nome dos Pagantes</th>
                    <th scope="col">Ações</th>
                </tr>
            </thead>
            <tbody>
                <% for (let item of resultado) { %>
                    <tr>
                        <td><%= item.Cliente %></td>
                        <td>
                            <button class="btn btn-primary" onclick="toggleInfo('<%= item.Pagamento %>')">
                                Ver Detalhes
                            </button>
                        </td>
                    </tr>
                    <tr id="info-<%= item.Pagamento %>" class="d-none">
                        <td colspan="2">
                            <ul>
                                <li><strong>Valor:</strong> R$ <%= item.Valor %></li>
                                <li><strong>Tipo do Plano:</strong> <%= item.Tipo %></li>
                                <li><strong>Data do Pagamento:</strong> <%= item.Dt_pag %></li>
                            </ul>
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table>
        
        
        
       
    </div>
    <footer class="rodape"><b>&copy; Todos Direitos Reservados TitaniumFit</b></footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>